const letters = ['A', 'B', 'C', 'D'];

for (let index = 0; index < letters.length; index++) {
    const element = letters[index];
    console.log(element + " elemento en la posicion " + (index + 1));
}