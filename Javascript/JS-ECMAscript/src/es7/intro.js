/*ES7 en JavaScript

Operador de Exponenciación (**)
El operador de exponenciación proporciona una forma más compacta y legible de realizar exponentiación. */
javascript
const base = 2;
const exponente = 3;
const resultado = base ** exponente;
console.log(resultado); // 8


/* Array.prototype.includes
El método includes permite comprobar si un array contiene un determinado elemento, devolviendo true o false. */

const numeros = [1, 2, 3, 4, 5];
console.log(numeros.includes(3)); // true
console.log(numeros.includes(6)); // false

