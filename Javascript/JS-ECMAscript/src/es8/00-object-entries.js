const countries = {MX: 'Mexico', UY: 'Uruguay', AR:'Argenitna'};
console.log(Object.entries(countries));
console.log(Object.keys(countries));
console.log(Object.values(countries));

// Tarea
// Crea una función que reciba el objeto inventario y una función de comparación, 
// y devuelva un nuevo objeto que contenga solo las propiedades que cumplen la condición especificada por la función de comparación. 
// Por ejemplo, si la función de comparación verifica si la cantidad es mayor o igual a 5: